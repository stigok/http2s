#!/usr/bin/env node

var http2s = require('../http2s.js');

var options = {
  http: 8080,
  https: 8443,
  auto: false,
  logLevel: 5
};

// Run with custom settings
http2s(options, function (err) {
  if (err) {
    return console.error(err);
  }
});
